[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm drizzle-kit migrate && node seed-db.mjs && node dist/index.js"

[variables]
NODE_ENV = "production"
